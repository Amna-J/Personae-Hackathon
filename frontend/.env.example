# Personae frontend environment — copy to .env and fill in real values.
# .env is gitignored; never commit real secrets.

# Groq API key used by the browser-side chat (AIchat) page.
VITE_GROQ_API_KEY=
